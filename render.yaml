services:
  - type: web
    name: brain-tumor-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: sh -c "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
    plan: free